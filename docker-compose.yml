version: "3.9"

services:
  insta-bot:
    build: .
    restart: unless-stopped
    volumes:
      - ./sessions:/app/sessions
      - ./media:/app/media
      - ./logs:/app/logs
    env_file: .env
    environment:
      - TZ=UTC
    deploy:
      resources:
        limits:
          memory: 512M
